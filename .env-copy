#Don't forget to delete the suffix -copy, the file should be named .env
DB_PASS=<db-password>
DB_NAME=<db-name>
DB_VERSION=<db-version> #e.g. 12

ODOO_VERSION=<odoo-version> #e.g. 14
ADMIN_PASS=<admin-password>

SUBDOMAIN_URL=<subdomain-url>
#If you have a domain example.com, through your domain provider add an "A type" register.
#e.g. odoo.example.com pointing to your public IP where your odoo is, with the nginx config it will redirect.